# Environment configuration for Game Task Emulator systemd service
# Copy this file to /etc/default/gametask-emulator and customize as needed

# Team city code (e.g., DAL, CHI, BOS) - leave empty for Dallas Stars (default)
# You can specify multiple teams separated by commas: TEAM_CODE=DAL,CHI,BOS
TEAM_CODE=

# Additional flags to pass to the application
# Default: -today (gets today's upcoming games)
# Options:
#   -today          : Get today's upcoming games only
#   -all            : Get all games for the day
#   -prod           : Use production queue instead of local emulator
#   -date YYYY-MM-DD: Specific date to query
ADDITIONAL_FLAGS=-today

# Google Cloud credentials path (if using production mode)
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account-key.json

# Discord webhook URL for notifications (optional)
# When configured, the application will send notifications to this Discord channel
# Get the webhook URL from Discord: Server Settings -> Integrations -> Webhooks
# DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/YOUR_WEBHOOK_ID/YOUR_WEBHOOK_TOKEN

# Container registry configuration (usually not needed)
# REGISTRY=ghcr.io
# REPOSITORY_OWNER=FirepowerApp
# REPOSITORY_NAME=gameTaskEmulator
